# 运动控制面试讲解笔记（基于你的真实代码）

> 素材来源：
> - `D:\Code\Github\csharp-note\Record_LeadShine_SLN\LeiSai3800Demo\Motion.cs`（雷赛 3800，本人编写）
> - `D:\Code\RMI\ImgApp-online\ImgApp\Services\SystemMotion`（公司项目运动控制模块）
> - `D:\Code\RMI\ImgApp-online\ImgApp\Services\DeviceService\Services\MotionControlService.cs`（异步封装）

## 一、你的真实底牌（先记住这个）

你之前说"项目里运动控制用得简单"，但从代码看，你实际已经写过这些，面试可以直接讲：

| 能力 | 代码证据 |
| --- | --- |
| 板卡初始化、多卡识别 | dmc_board_init、dmc_get_CardInfList |
| 点位运动（相对/绝对） | dmc_pmove |
| Jog/连续运动 | dmc_vmove |
| 速度曲线（梯形加减速） | dmc_set_profile |
| S 曲线 | dmc_set_s_profile |
| 直线插补（多轴） | dmc_line_multicoor |
| 两轴圆弧插补 | dmc_arc_move_multicoor |
| 插补合成速度 | dmc_set_vector_profile_multicoor |
| 回零（方向/模式/偏移/清零） | dmc_set_homemode、dmc_set_home_position、dmc_home_move |
| 限位（使能/电平/停止模式） | dmc_set_el_mode |
| 减速停止/急停 | dmc_stop、dmc_stop_multicoor |
| 状态查询 | dmc_check_done、dmc_get_position、dmc_axis_io_status |
| 工程封装 | 单例 + 轴名表 + IO 表 + 状态线程 + 超时保护 |

所以你的定位不是"只懂点位/Jog"，而是"点位、Jog 实战，插补、回零、限位都写过代码，公司项目里做了工程化封装"。这是 15k+ 上位机岗位能打的配置。

## 二、30 秒电梯版

我做过固高、雷赛、正运动、高川四种控制卡的点位和 Jog 运动，雷赛项目里我自己写了一套运动封装，覆盖点位、连续运动、直线插补、圆弧插补、回零、限位和 S 曲线。公司项目里把运动控制进一步工程化：单例管理、按轴名建轴表和 IO 表、10 毫秒轮询的状态监控线程，回零、点位、平滑停止、急停都有封装，还加了超时和位置公差保护。我对运动控制的理解不只是调 API，而是清楚背后的速度规划、坐标系、插补原理和安全联锁。

## 三、2 分钟完整版

我分三层讲运动控制：

第一层是单轴运动。每个轴的运动流程都是固定的：先初始化板卡、加载轴参数、建立轴和 IO 表；然后回零建立机械原点，回零要配置方向、原点信号电平和偏移量；之后才能做点位运动。点位分相对运动和绝对运动，运动前用速度曲线函数设好起始速度、最大速度、加减速时间，需要平滑过渡时加 S 曲线参数。

第二层是多轴插补。多轴联动要先分配坐标系，再设置合成速度。直线插补是把多个轴的终点位置一起下发，控制卡内部按插补算法让各轴同时到达；圆弧插补需要指定终点、圆心和圆弧方向。插补的本质是控制卡在坐标系里做轨迹规划，把连续轨迹离散成每个采样周期的脉冲。

第三层是工程化封装和安全。公司项目里我把运动控制封装成单例，轴、输入、输出都用名字建表，业务层不直接面对轴号；另外启动一个常驻线程每 10 毫秒轮询所有轴状态，回零、运动、报警都从这里汇总。设备还做了软硬限位、安全门联锁、急停、超时保护，运动完成后检查位置公差，不达标就报错重试。这也是 X-Ray 设备为什么敢做无人值守自动检测的基础。

## 四、运动控制原理到底指什么

一句话：运动控制原理 = 让轴"按指定速度曲线、沿指定轨迹、准确到达指定位置"背后的概念体系。分四块：

### 1. 单轴运动

- 点位运动：从 A 到 B，只关心终点，中间轨迹不管（dmc_pmove）
- Jog/连续运动：持续输出速度直到停止（dmc_vmove）
- 速度曲线：梯形加减速（起始速度、最大速度、加减速时间），S 曲线让加加速度连续，运动更平滑
- 回零：用原点信号（ORG）或限位信号建立机械原点，分"回零方向、速度、模式、偏移量、清零"几个参数
- 限位：正负限位信号，分立即停止和减速停止

### 2. 坐标系与多轴

- 坐标系：把多个轴绑定成一个插补坐标系（crd），才能做联动
- 直线插补：多轴按比例分配速度，同时到达目标点，走直线
- 圆弧插补：指定终点、圆心、方向，控制卡内部做圆弧轨迹
- 合成速度/合成加速度：插补时设置的是轨迹上的矢量速度，不是单轴速度

### 3. 伺服系统（面试高频）

- 控制模式：位置模式（上位机发位置指令）、速度模式、扭矩模式，设备里一般用位置模式
- 编码器：反馈实际位置，分增量式和绝对式
- 电子齿轮比：上位机脉冲和电机转动的换算关系
- 脉冲当量：每个脉冲对应的机械位移，由丝杠导程、减速比、编码器分辨率算出

### 4. 安全与联锁

- 急停（硬急停 + 软件急停）
- 软限位（软件里限位置）、硬限位（限位开关）
- 安全门联锁：门开禁止运动
- IO 互锁：气缸、传感器、夹具与轴动作顺序互锁

## 五、公司项目封装怎么讲

面试官会问"你项目里的运动控制怎么写的"，按这套讲：

1. 单例模式：`TotalMotionUnit.Instance`，整个程序只有一份运动控制实例，避免多窗口操作同一张卡。
2. 配置驱动：`OpenCard()` 里先加载运动数据（轴参数、速度参数），再打开控制卡、初始化、按配置建轴表、输入表、输出表，业务层用轴名（如 WidthY）而不是轴号访问。
3. 状态线程：`StartMotionStatus()` 启动一个常驻 Task，每 10 毫秒轮询轴状态、报警、IO，运动状态集中在这里更新。
4. 异步封装：`MotionControlService` 把运动封装成 async 接口，比如 `AdjustTrackWidthAsync`：先回零、轮询等待完成（带 60 秒超时）、到位后检查位置公差（0.5mm），失败平滑停止并报错。
5. 安全：复位用 `SemaphoreSlim` 加锁防止并发复位，门状态联锁，所有异常路径都调 `ALLMotionStop`。

讲这套的好处：说明你不只会调 API，还懂单例、异步、超时、异常恢复这些工程问题，这正是小公司和大厂上位机岗位的差距点。

## 六、面试追问准备

### 1. 点位运动用相对还是绝对？

看场景。相对运动适合重复等距动作（每步固定距离），绝对运动适合固定工位定位（目标位置已知）。设备里一般用绝对运动，配合回零保证坐标系一致；运动中要不断查询是否到位。

### 2. 梯形加减速和 S 曲线什么区别？

梯形：速度线性上升、匀速、线性下降，加速度突变，机构会有冲击；S 曲线：加加速度连续，速度过渡更平滑，适合高精度、怕振动的设备。代价是算法复杂、运动时间略长。

### 3. 直线插补的原理？

把目标位置按插补周期离散，各轴按轨迹方向的比例分配速度，保证每个采样点都落在目标直线上，各轴同时启动、同时到达。控制卡内部完成，上位机只需下发终点和合成速度。

### 4. 轴走不准怎么排查？

顺序：先看机械（丝杠松动、打滑、负载），再看编码器反馈（实际位置和指令位置差多少），再看速度曲线（加减速太陡导致失步），最后看参数（脉冲当量、电子齿轮比、限位触发位置）。设备里 90% 走不准先查机械和回零漂移。

### 5. 轴不动怎么排查？

顺序：使能是否打开 -> 是否报警 -> 是否被限位挡住 -> 急停是否按下 -> IO 互锁是否满足 -> 速度参数是否为 0 -> 控制卡连接是否正常。这也是你现场调试每天干的事，可以讲一个真实案例。

### 6. 为什么设备要先回零才能运动？

绝对位置依赖坐标系原点。不回零，所有轴的实际原点位置不确定，定位全是错的，还可能撞机。回零是用原点信号或限位信号把机械位置和软件坐标对齐。

### 7. 急停和减速停止区别？

减速停止按设定减速度平滑停，机械冲击小，但要一段距离；急停立即停，为了安全可以牺牲冲击。设备上硬急停走控制卡紧急停止模式，软件逻辑出错时也要能调停止接口。

### 8. 状态线程为什么用 10 毫秒轮询而不是事件？

控制卡是查询式 API，没有现成的事件通知；用常驻线程按固定周期轮询，简单可靠，10 毫秒对设备运动监控足够（运动周期一般几十毫秒以上）。轮询线程要处理竞态，所以共享状态要加锁或用线程安全结构。

## 七、排查套路（现场故事素材）

每个套路配一个你经历过的真实案例，用 STAR 讲：

1. 轴不动：使能 -> 报警 -> 限位 -> 急停 -> IO -> 参数 -> 连接
2. 走位不准：机械 -> 编码器 -> 速度曲线 -> 参数 -> 回零漂移
3. 抖动/异响：加减速太陡 -> S 曲线 -> 机械刚性 -> 伺服增益
4. 偶发报警：先看报警码 -> 看时序（IO 抖动）-> 看干扰（线缆/地线）-> 加滤波

## 八、练习动作

1. 打开 `Motion.cs`，把上面表格里的每个 API 和注释再读一遍，确保每个函数都能说出"干什么、参数是什么、什么时候调用"。
2. 看 `MotionControlService.cs` 的 `AdjustTrackWidthAsync`，理解"回零 -> 运动 -> 轮询到位 -> 检查公差 -> 超时保护"这个完整闭环，这是你最值得讲的工程案例。
3. 录音讲 30 秒版，再讲 2 分钟版，卡壳的地方就是你要补的概念。
4. 准备一个 STAR 故事：现场出现过"轴不动/走位不准"，你怎么排查解决的（情境、任务、动作、结果）。
