---
description: 通用协作与实现规范入口（动手闸门、UI 硬约束、Git）
alwaysApply: true
---

# 本仓库规范

完整正文：[PROJECT_STANDARDS.md](../../PROJECT_STANDARDS.md)。冲突时先指出差异，再按用户明确选择执行。

**握手（必须）：** 每段新对话的**第一条回复开头**必须是「牢大」。后续回合不要重复。用户看不到这三字 = 规范未加载。

## 闸门

- 「分析 / 先看看 / 你觉得呢 / 先别改」→ 本回合不改文件。
- 同句既有「修」又有「先分析」→ 只分析，末尾再问是否落地。
- 匹配的 Skill 先 Read 再执行；不要虚构 Skill；不要主动 `SwitchMode`。
- 声称完成前须有验证证据；默认不 commit / push / PR。

## 实现时记住

- 大改先对齐方案；Bug 先调用链再改码。
- 设计先四问（复用 / 变化 / 痛点 / 是否值得抽象）；单一写权威；禁止读写键不一致。
- 临时产物用 `%TEMP%` 或 `_build_verify/`，用完即删，不得提交。
- UI（WPF / WinForm / HTML）：语义色与字号、禁止贴边、禁止默认横滚、大改先 HTML 设计稿（`docs/mockups/`）、非 UI 线程不碰控件、成功用 Toast、不可忽略用模态。
- Commit 用中文 `type:` 句号结尾；不要为对齐风格翻 `git log`。
