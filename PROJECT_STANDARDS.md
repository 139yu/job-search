# 项目开发规范（通用）

> 从 `ImgApp-online/PROJECT_STANDARDS.md` 抽取**跨项目仍成立**的约定，作为本仓库 Agent 协作与实现的权威来源。  
> **不收录**：ImgApp 模块表、Prism/HandyControl/SqlSugar、Halcon/FPD 等专有架构。那些只在对应代码仓库生效。  
> 新增或修改规范请只改本文件；`AGENTS.md` / `CLAUDE.md` / `.cursor/rules/` 只做索引。  
> 用户明确指令与本规范冲突时：先指出差异并请用户确认，再按用户选择执行。

**本仓库定位：** 求职资料库 + 脚本/文档/HTML 工作区，不是可部署应用。规范约束本仓库内的改动，以及在此讨论的 WPF / WinForm / HTML 设计工作。

---

## 目录

1. [对话与协作](#1-对话与协作)
   - [§1.0 规范加载握手](#10-规范加载握手必须)
2. [动手闸门](#2-动手闸门)
3. [设计深度](#3-设计深度)
4. [临时产物](#4-临时产物)
5. [命名](#5-命名)
6. [UI 通用原则](#6-ui-通用原则wpf--winform--html)
7. [注释、日志、异常](#7-注释日志异常)
8. [Git](#8-git)
9. [验证](#9-验证)

---

## 1. 对话与协作

### 1.0 规范加载握手（必须）

**每段新对话的第一条回复，开头必须喊「牢大」。** 这是规范已加载的握手信号；用户看不到这三字，即视为本轮未吃到 `PROJECT_STANDARDS.md` / Cursor 规则。

| 项 | 约定 |
|----|------|
| 时机 | 仅**本对话第一条**助手回复；后续回合不要重复喊 |
| 写法 | 回复**最开头**就是「牢大」（可紧跟逗号或换行再写正文） |
| 禁止 | 改成别的口头禅、埋在段中、用英文替代 |
| 续聊 | 同一会话内用户继续提问 → 不再喊；**新开对话**再喊一次 |

1. **先对齐再大改**：范围、根因、方案未清时，优先分析与提问；用户明确要改再动手（§2）。
2. **少形式、多实质**：不要求每句闲聊都写规格文档；有可验收决策时，用聊天确认或简短方案即可。
3. **匹配的 Skill 要用**：`available_skills` 中与当前任务匹配的 → 先 Read `SKILL.md` 再执行。没有匹配的不要虚构 Skill 名。用户未发起的斜杠插件流程，不要主动推销。
4. **已启用的流程要跟完**：用户已发起某一 Skill / 斜杠流水线时，按该流程做到实现步；未到实现步不得改落地文件。用户明确「不再用该流程」后，回到普通协作。
5. **不主动切 Cursor 模式**：禁止 Agent 调用 `SwitchMode`；Ask 模式只读；用户若进入 Plan，其确认不等于自动改码许可。
6. **代码探索**：有 CodeGraph 时优先用；不可用再 Grep / Glob / Read。

### 1.1 Bug / 交互异常

涉及缺陷、交互不符预期、跨模块行为时，**不得**未理清就堆补丁。

1. 对齐现象（期望 vs 实际、复现步骤）。
2. 梳理调用链（入口 → 状态 → 显示/落盘）。
3. 书面根因（为何出现；与先前补丁关系）。
4. 方案与范围 → 用户确认或明确「直接修」后再改。
5. 按 §9 验证；禁止未验证声称修好。

同一问题连续两轮仍不对：停手，回到 1–4，未经确认不大面积改。

**禁止：** 未读调用链就改坐标/重绘；多轮盲试补丁；根因未明声称已修好；把「你觉得呢」当成改码口令。

### 1.2 新增与重构

| 体量 | 建议 |
|------|------|
| **小** | 意图对齐 → 建议做法 → 用户确认 → 改码 |
| **大**（多模块 / 改权威 / 通用组件） | 书面 2～3 方案 + 利弊 + 推荐 → **确认前不大规模改码** → 再落地；设计深度见 §3 |

大 UI 可用 HTML 设计稿（§6.6）作确认物。用户只要分析不要代码时，停在方案步。删除弃用能力须用户确认。

**规格落盘：** 聊天确认或简短方案即可。禁止为走过场新建大套 `specs/` / `plans/` 产品文档（用户明确要求除外）。

### 1.3 验证与收尾

声称「完成 / 修好」前须有新鲜证据：改了可运行代码则按 §9 验证；有任务清单则按其勾选。默认不自动 commit（§8）。**push / PR** 仅用户明确要求。

---

## 2. 动手闸门

**本回合禁改业务文件**（用户意图含下列任一）：

| 信号 | 应做 |
|------|------|
| 「分析一下」「先看看」「为什么」 | 只读 + 结论 |
| 「你觉得呢」「给个意见」 | 只给意见 |
| 「先别改」「不要改代码」 | 停手 |
| 「整理规范」（未说写入） | 只建议；写入须明确口令 |
| Cursor **Ask** | 禁止任何改文件 |

同句既有「修一下」又有「先分析」→ **本回合只分析**，末尾问是否落地。

**可以改：** 用户明确「改 / 修 / 实现 / 落地 / 按这个做 / 整合到当前项目」等。

**不算开工：** 单独附和「嗯 / 好 / 可以」；仅选定方案未说实施。

---

## 3. 设计深度

新增 / 重构时自检这四问（完整方案开头显式回答；小改 Agent 自检即可）：

1. **复用**：是否会在其它入口再次出现同类能力？
2. **变化**：会变的是规则、结构、创建，还是通知 / 生命周期？
3. **痛点**：主要痛在数据权威、会话边界，还是纯交互？
4. **模式**：引入分层 / 设计模式是否明显降低后续改动成本？不值则保持简单。

深度与是否抽象 **先与用户沟通**，Agent 不单方面拍板。

**模式是备选，不是打卡。** 按「变什么」选家族；禁止「别处用了某模式，此处无条件照搬」。

### 3.1 运行时与持久双轨

同一语义既有运行时对象又有可序列化表示时：

- 明确 **单一写权威**。
- 约定会话开始 / 结束 / 取消时谁覆盖谁；空持久不得静默清空非空运行时（除非产品明确「清空」）。
- **禁止** 多处各持一份互不同步的「当前数据」。
- 落盘键与显示读取键必须一致。

### 3.2 反模式

- 写入键 A、显示或下游读键 B。
- 无真实消费者却先抽象 `I*` / 空框架。
- 全局 static 与会话状态双轨并存且职责不清。
- 用连续补丁掩盖权威不清，代替收口单源。
- 在异步 / 非 UI 线程直接操作 UI（§6.7）。

---

## 4. 临时产物

Agent / 脚本为调试、验证而在**仓库工作区内**创建、不属于源码的文件或目录，用完即删。

1. **优先不污染工作区**：能写 `%TEMP%` 或约定验证目录的，禁止在仓库根随手新建目录。
2. **固定目录复用**：编译验证只用仓库根 `_build_verify/`（已 gitignore）；禁止每次换新目录名。
3. **用完必删**：声称完成前删除本轮临时文件；不得留给用户「以后自己删」。
4. **不得提交**：临时产物、密钥、一次性 dump 不得 `git add`。
5. **收尾自检**：任务结束前 `git status`，确认无大量未跟踪的构建输出。

`_build_verify/` 可跨会话保留以便复用，但**不提交**。

---

## 5. 命名

- **跟随周边**：无强制 editorconfig 时，与同目录现有文件保持一致。
- **见名知意**：能短则短，但不能短到猜不出；禁止 `DoIt`、`Temp1`、难认缩写。
- **去掉重复语境**：模块 / 命名空间已表达的前缀后缀不要再叠。
- **文件名 = 主类型名**；缩写后同步改文件名。
- **同一概念同一缩写**（如 `Desc` / `Param` / `Config` / `Max` / `Info`）；同一功能域勿混用两套。存量全称不必为统一而大面积重命名。
- 类按职责落盘：页面进 Views、绑定进 ViewModels、展示模型进 Models、工具进 Helpers；**没有文件夹就建，不要乱放**。不确定归属先问，禁止因为「改着方便」塞进不相关模块。

---

## 6. UI 通用原则（WPF / WinForm / HTML）

视觉决策优先走已安装的 **UI/UX Pro Max** skill。本节是跨栈硬约束，不绑定某一控件库。

### 6.1 主题语义，禁止硬编码

- 禁止硬编码颜色（`#RRGGBB` / `Color.FromRgb`）和字号魔法数。
- 使用主题 / 资源字典 / CSS 变量等**语义键**（背景、前景、边框、强调、状态）。
- 新颜色成对补亮/暗主题（若项目有主题切换）；命名体现用途，禁止按页面命名（如 `CncOnlyBg`）。
- 按钮语义用样式，不写死背景：主操作 / 次要正向 / 破坏性 / 中性。

### 6.2 留白（禁止贴边）

可见内容不得紧贴容器边框或单元格边线。

| 层级 | 约定 |
|------|------|
| 容器最小留白 | 一般 ≥ 8px；侧栏、属性表、对话框内容区优先 10–12px |
| 子项间距 | 相邻控件/行 4–8px；标签与输入之间留间距 |
| 同页表单 | 标签列等宽、输入列等宽；某一控件特宽须注释说明 |
| 表格 | 同一张表表头与单元格水平对齐必须统一 |
| 例外 | 全宽分割线、画布、刻意贴边的装饰线；须注释且不影响可读性 |

### 6.3 悬停 / 选中

列表行、表格行等**非主按钮**交互态，用半透明灰叠在底色上，不要用主题蓝/绿实色块，以免与主按钮、开关、徽章撞色。强调色留给可点击主操作与状态。

### 6.4 自定义矢量图标

新建线框矢量（非字体图标、非位图）：统一目录 + 统一键名；禁止在页面内联大段 Path。显示用 Viewbox 等比缩放设计坐标描边，禁止裸 `Stretch=Uniform` 裁切描边。

### 6.5 禁止水平滚动条（除非明文允许）

默认禁止横向滚动。视口变窄时用换行、省略、列宽自适应或纵向滚动消化。例外须在需求/设计稿/注释中写明原因。

### 6.6 大 UI 先 HTML

**布局变动较大**或**交互流程需确认**时：先出可预览 HTML，用户确认后再改 WPF / WinForm / 正式页。

| 需要 HTML | 可不做 |
|-----------|--------|
| 新页面 / 侧栏 / 主从列表等结构级调整 | 改文案、图标、边距微调 |
| 多步骤交互、折叠、预览路径需确认 | 已确认方案的纯逻辑修补 |
| 多方案对比供点选 | 与现网几乎一致的小补丁 |
| 用户明确要求先出设计稿 | 几乎不改布局的数据/权威修改 |

**保真：** 区块顺序、主从关系、按钮文案、空态、控件角色与拟实现一致；遵守 §6.2 / §6.5；浅色、克制，禁止另起炫技皮肤冲淡评审；画布/硬件用示意区 + 文字，勿伪装已接入。

**本仓库存放：** `docs/mockups/`，命名 `功能-场景.html`，单文件优先。默认不提交；用户要求再另议。若目标代码仓库已有预览脚本，**必须用该脚本**，禁止手搓随机端口的临时服务器。

小改（文案、边距、纯绑定）不必强制 HTML。

### 6.7 异步与 UI 线程

在 `async` / 线程池 / 硬件回调等非 UI 线程上：**禁止**直接操作控件、改绑定集合、弹窗。必须更新 UI 时先切回 UI 线程。面向用户的提示同样须经 UI 线程；打日志则任意线程均可。

### 6.8 用户提示：轻量 vs 必须确认

| 场景 | 通道 |
|------|------|
| 操作成功、无后续决策 | 非阻塞提示（Growl / Toast / Snackbar） |
| 一般提示、可稍后处理 | 非阻塞 Info / Warning |
| 删除 / 覆盖 / 不可逆前确认 | 模态确认 |
| 用户不可忽略的失败（硬件、数据损坏、权限、流程阻断） | 模态错误/警告 |
| 须阅读后再继续的说明 | 模态 |

**原则：** 不拦操作、知道即可 → Toast；必须点一下才能继续 → 对话框。业务/硬件 Service **禁止**直接弹 UI；由界面层在边界提示。弹窗文案禁止堆栈，详请写日志。

---

## 7. 注释、日志、异常

### 7.1 注释

- **新建 / 实质改动**：对外 API、业务方法、有语义的属性须有文档注释（C# XML / Python docstring / JSDoc，跟语言惯例）。
- **存量**：改到再补，禁止为「补注释」大范围空翻文件。
- **可省略**：名称已自明的私有短胶水、纯 DI 注入字段、原样加载无二次加工的选项集合。
- 非自明数值提取为命名常量。
- 大块逻辑用分段注释或提取 helper，不按行数机械拆分。

### 7.2 日志

| 级别 | 场景 |
|------|------|
| Info | 正常流程、关键用户操作 |
| Warn | 可恢复异常、降级 |
| Error | 不可恢复错误（必须带异常对象） |

前缀建议：`[模块/功能] 操作: key=value`。禁止空 catch、日志含密码/连接串、高频 Info（定时刷新用 Debug）。

### 7.3 异常

禁止因可预期业务异常导致进程未处理退出。全局钩子只是最后防线，不可替代界面边界的显式处理。

- UI / 写操作边界：`try-catch`；向用户提示友好文案，**禁止**直接展示堆栈。
- `catch` 必须打 Error 日志并带异常对象。
- 多步写操作须在同一事务内完成（有则用；无则在方案里写清失败补偿）。
- 捕获具体类型；允许降级时注释原因。

### 7.4 可复用组件

新增面向多处复用的通用组件 / Helper 时，**必须随代码提供接入说明与最小示例**；公开 API 或默认行为变更时同一改动内更新文档。文档与实现不一致视为未完成。

---

## 8. Git

### 8.1 何时 commit

交的是「可验收的一整块」，不是「任意一次改码结束」。

| 情形 | Agent 行为 |
|------|------------|
| 用户明确说「提交 / 交一下」 | 可以按指示 commit |
| 用户说「先别提交」 | 禁止 commit |
| 未明确要求 | **默认不自动 commit**；收口后可询问是否提交 |
| 密钥、临时产物、默认不提交的 mockup、`_build_verify` | 禁止纳入 |

**push / PR** 仅用户明确要求。PR 正文：中文摘要 + 可勾选测试计划，不写空洞套话。

### 8.2 Commit 消息

Agent **禁止**每次提交前翻 `git log`「对齐近期风格」；确认改动后按本节起草。

```
<type>: <一句话说明做什么/为什么>。

[可选第二段：1～2 句补充范围、顺带项或风险]
```

- 语言：**中文**（专有名词 / 路径可保留英文）。
- 主句建议以「。」收尾；写为什么 / 行为结果，少写文件清单。
- 前缀首选：`feat:` / `fix:` / `chore:` / `refactor:` / `docs:`。极短纯文案微调允许无前缀；新提交优先带 `type:`。
- 禁止：英文整句当主说明、`update` / `临时` / `改一下`、把无关大改塞进同一条。

---

## 9. 验证

| 改动类型 | 验证方式 |
|----------|----------|
| 可测纯逻辑 | 能测则测；有测试命令则跑，回复里给证据 |
| UI 布局 / 交互 / 设备闭环 | 编译或脚本可跑则跑 + 手测步骤写清；不硬造脆弱 UI 自动化 |
| 仅文档 | 检索 / 锚点核对即可，免编译 |

声称完成前须有 §1.3 的新鲜证据；回复简述「测了什么 / 未测什么」。

---

*来源：ImgApp-online `PROJECT_STANDARDS.md` 的通用子集。专有实现细节仍以各代码仓库规范为准。*
