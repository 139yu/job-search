<Window x:Class="MainApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApp"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="900" Width="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.6*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <!--菜单区域-->
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="AliceBlue">
            <ItemsControl ItemsSource="{Binding MenuItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <!--左侧区域-->
        <Grid Grid.Row="1" Grid.Column="0" Grid.RowSpan="2" Background="Aqua"></Grid>
        <!--Halcon 窗口-->
        <Grid Grid.Row="1" Grid.Column="1" Grid.RowSpan="1" Background="DarkBlue"></Grid>
        <!--motion区域-->
        <Grid Grid.Row="2" Grid.Column="1" Grid.RowSpan="1" Background="BlueViolet"></Grid>
        <!--右侧区域-->
        <Grid Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" Background="DeepSkyBlue"></Grid>
    </Grid>
</Window>
